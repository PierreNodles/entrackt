{% extends 'base.html.twig' %}

{% block title %}Critique{% endblock %}

{% block body %}
    <h1>Critique</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ critique.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ critique.title }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ critique.slug }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ critique.image }}</td>
            </tr>
            <tr>
                <th>Critique</th>
                <td>{{ critique.critique }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('critique_index') }}">back to list</a>

    <a href="{{ path('critique_edit', {'id': critique.id}) }}">edit</a>

    {{ include('critique/_delete_form.html.twig') }}
{% endblock %}